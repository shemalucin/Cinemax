/// <reference types="react" />
/// <reference types="react-dom" />
/// <reference types="vite/client" />

// Ensure TypeScript recognizes JSX intrinsic elements for the admin app.
// If your editor still shows JSX typing errors, reload the window or run
// `npm run lint` in the admin folder to see updated diagnostics.
